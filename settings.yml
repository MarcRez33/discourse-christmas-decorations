enable_lights:
  default: true
  description: "Enable lights?"

enable_hats:
  default: true
  description: "Show hat on avatars?"

enable_decoration_image:
  default: true
  description: "Show decoration image?"

decoration_invert_color:
  default: false
  description: "Invert decoration image color? Enable this on dark theme."

enable_fireworks:
  default: true
  description: "Show fireworks?"

enable_fireworks_follow_screen:
  default: true
  description: "Should fireworks follow the screen on scroll?"